# DjangoCustomerApiWithJwtAuth

open cmd/terminal

create virtual environment

`pythom -m venv env`

activate virtual env

`env/script/activate`

`pip install -r rquirements.txt`

to run docker

`docker-compose up --build`

Run local server

`127.0.0.1:800` or `localhost:8000`

